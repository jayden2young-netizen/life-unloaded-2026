# 《人生尚未加载 · 2026》当前交接

更新时间：2026-08-22。这里只记录换窗口或换电脑后立即需要的当前事实，不保存版本历史和通用开发规则。

## 当前快照

| 项目 | 当前事实 |
|---|---|
| Git | 临时分支 `codex/fable-v070-integration`，当前 HEAD `dfd5fc2`；Revision 35 修复仍在未提交工作树，未 push、PR 或合并 |
| 版本 | 本地候选 v0.7.0／Schema 14／Content Revision 35；公开版仍为 v0.6.13 |
| 内容 | 965 个事件：517 beat、214 decision、214 consequence、20 blackSwan；64 个生成 episode ID／44 个目录定义、83 张卡牌、90 个结局标题 |
| 数据 | `data.json` SHA-256：`02f966360ef2e40a579eaae6a9587436ceba7abb4fbdee8ca8af61bc7332f2fe` |
| 发布 | 未发布；五轮真人验收前只称 v0.7.0 候选 |
| 验证 | Revision 35 正式生成器双跑 byte-stable；`--fast` 与相关 profile 通过；最终 `node smoke.js --full` 仅运行一次且全部八组通过；四档视口控制台错误为零 |

## 当前已完成

- 普通选择目标为种子稳定的 22—28，按婴幼儿、童年、青少年、青年、成家立业、中年、晚年分配；童年与 75 岁后均保留合法选择机会。
- pity 年龄、候选寿命与选择目标均由种子决定；黑天鹅没有保底配额，recurrence 同组间隔仍为三年。
- Schema 14 增加态度、属性提交、亲人离世时间、平台／经营年数和跨局开关；升级时清除活动人生并保留档案、图鉴、设置、seen 与 stats。
- 跨局已接入事实化上一局、动态收集进度、种子重开和默认关闭的隔世回声；不显示原始六轴，也不给数值收益。
- 12 类主题结局优先于无主题早亡；near miss 只在有事实接近阈值时出现，`wealthApex` 仍不提示“差一点”。
- `parent_loss` 按 214→154→215→216 推进，并绑定首次触发的具体父母与年龄；第二位父母随后离世不会重置原 episode。
- 60 条态度微选择支持选择、跳过、刷新恢复和时间线附注；不足五次不生成结局侧写。
- 六项 raw attrs 可确定性漂移，presence／drive／composure 同步派生；十张新代价卡的持续效果从持卡动态派生并限制在明确事实路径。
- C1—C9 与 N1—N7 候选已逐条整合和校正；住房、健康、受雇、亲人离世、图鉴和结局文案均服从真实状态，不把内部合同写给玩家。
- 三轮真人 UI 试玩的六个 P1 已修复：首位父母离世当年同步、职业场景有限集合、职业空档保留继续工作权与原领域回归、本科旧程序清理、childfree 统一承认、年度队列出队复验。
- 活动人生时间线现在保留全部新记录并可向上回看；滑动不触发年龄推进；结局页在移动与桌面视口均可滚到底部操作。
- 修复后已完成两层独立只读审查；唯一疑点经主代理查证为不可达旧标签假设，无可执行遗留项。
- roadmap、调查案卷与实现规格继续保留在 `_bmad-output/` 或 `roadmap/` 的本地忽略边界，不进入 Git。

## 下一步

- 三轮真人人生已完成，发布验收仍为 3/5；修复后还需补两轮从出生到死亡的正常阅读验收。
- 一轮优先观察 75+ 合法选择和晚年有效内容；一轮观察修复后的 childfree、parent_loss、职业切换和至少一种持续代价卡，仍不修改存档强制事件。
- 若普通局明显超过 20 分钟或复杂局普遍超过 25 分钟，只把 M2 总目标回退到 18—24；若只是晚年被拉长且有效内容不足，只回退 M7。五轮通过前不得进入 push、合并或发布阶段；本轮 Revision 35 修复也尚未 commit。
