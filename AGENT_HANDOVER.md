# 《人生尚未加载 · 2026》当前交接

更新时间：2026-08-15。这里只记录换窗口或换电脑后立即需要的当前事实，不保存版本历史和通用开发规则。

## 当前快照

| 项目 | 当前事实 |
|---|---|
| Git | `main` 与 `origin/main` 同步；`legacy/0.6.11@fdeaed3` 保存 Revision 31 最终公开版 |
| 版本 | 线上／本地均为 v0.6.12／Schema 13／Content Revision 32 |
| 内容 | 910 个事件、205 个选择面板、62 个 episode、214 条卡牌互动、73 张卡牌 |
| 数据 | `data.json` SHA-256：`af1e0cf208a90bc16e637587e3f682d449b85492152eaa0d60305b3c982e7a97` |
| 发布 | GitHub Pages 已发布；线上 `index.html`、`game.js`、运行时合同与 `data.json` 已和发布提交核对一致 |
| 验证 | Revision 32 生成器双跑一致；`node smoke.js --full` 全部通过，320×568／360×640／360×773 代表路径控制台错误为 0；按用户决定未重复三轮发布前全人生 UI 实测 |

## 当前已完成

- v0.6.12 已把可选 episode start 与独立选择合入统一普通池；候选探测和下一选择预览不再推进 RNG，刷新与恢复保持一致。
- 14 岁认领和成年重排各自最多保护一个具体入口。保护可越过阶段 allowance 与原 16—20 target，但普通候选和危机不能占用加座；硬资格、拒绝权和失败结果保持有效。
- `mainConflict` 仍表示开局长期张力；claimed desire 只通过具体 `opportunity` 元数据响应，不建立整轨道万能映射、持久 focus 或机会队列。
- 家庭规划窗口内的隐藏关闭已删除；晚成伴侣只给一次诚实回响，不扩大生育年龄或新增家庭成功路线。
- 职业考证 route、旧同事工作史、工资／余额事实、晚年婚姻保级、无子女边界卡及同类事实前提已在作者源、生成器或运行时的正确层级修复。
- `episodeCatalog` 保持 41 项稀疏 override registry；15 个习惯 episode 与 6 个短簇由显式 exception inventory 和完整 62-ID 合同覆盖。
- 第三方审阅稿、roadmap、调查案卷与实现规格继续保留在 `_bmad-output/` 或 `roadmap/` 的本地忽略边界，不进入 Git。

## 下一步

- 下一开发版本是 v0.6.13：动态年度密度、时间线层级、主线／结局辨识度和重玩性；开发前必须重新测量“16—20 原目标＋0—2 定向保护＋既有高层超额”，不能把 protect 加座重复算成普通扩容。
- v0.6.13 默认保持 Schema 13／Revision 32；只有实际修改生成玩家内容时才升至 33。寿命、死亡概率、18—24 整体预算候选和终局轴仍需新的产品确认。
