# Life Unloaded 项目入口

本文件只负责路由和硬边界。不要把 README、版本历史、roadmap 或写作规范复制到这里。

## 每次开始

1. 先读本文件，再检查 `git status --short --branch`、当前分支和最近提交；现场代码、版本常量、生成数据与测试结果高于文档摘要。
2. 读 `AGENT_HANDOVER.md` 获取当前版本、验证状态、下一步和阻塞项。
3. 按任务补读唯一事实源：
   - 跨系统状态、数据和测试契约：`docs/project-context.md`
   - 玩家中文写作、改写或审阅：完整阅读 `CopyWriting_Guideline.md`
   - 当前版本范围与产品决定：`roadmap/00-总体策略.md` 和对应版本 roadmap
   - 历史版本：`progress.md`；研究只在相关领域需要时读取 `docs/research/`
4. 规划、研究、实现、测试、提交、推送、部署和删除分别授权，不从前一步自动推导下一步。

`roadmap/` 与 `_bmad-output/` 是本地资料。换电脑后若不存在，不凭记忆补写，也不把交接摘要当成完整规格；需要版本规划或追溯审阅时，先请用户提供原文件。

## 玩家文案门槛

涉及玩家可见中文时，必须执行 Writing Guideline 第 10 章：动笔前留下六项“写作凭据”，交付时给出应用证据。只声明“已读”不算完成。

技术事实只是护栏。不得把 route、phase、变量、结算、兼容性或作者的安全说明转述给玩家。例句用于理解判断，不是句式模板。

## 永久边界

- 保护用户已有修改和本地资料。`roadmap/`、`_bmad-output/` 与第三方分析保持现有本地边界，不因开发或发布自动纳入 Git、移动或删除。
- 玩家内容改作者源；`data.json` 只由正式生成器生成，不得手改。
- 两种以上产品行为都合理、现实制度与游戏规则冲突，或必须改变 Schema、状态含义、年龄、概率、路线结果、选项数量和玩家权利时，停止并交回用户。
- 正常验证按风险选择一个相关静态检查和一条代表路径；发布或共享基础设施变化才运行 `node smoke.js --full`。不运行批量人生模拟，除非用户明确要求。
